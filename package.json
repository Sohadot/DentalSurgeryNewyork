{
  "name": "dental-surgery-new-york",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "typecheck": "tsc --noEmit",
    "validate:content": "tsx scripts/validate-content.ts",
    "validate:routes": "tsx scripts/validate-routes.ts",
    "validate:seo": "tsx scripts/validate-seo.ts",
    "validate:cost-content": "tsx scripts/validate-cost-content.ts",
    "validate:cost-data": "tsx scripts/validate-cost-data.ts",
    "validate": "npm run typecheck && npm run validate:content && npm run validate:routes && npm run validate:seo && npm run validate:cost-content && npm run validate:cost-data",
    "build:index": "tsx scripts/build-content-index.ts",
    "build:sitemap-manifest": "tsx scripts/build-sitemap-manifest.ts",
    "refresh:reference-data": "npm run build:index && npm run build:sitemap-manifest",
    "prebuild": "npm run validate && npm run refresh:reference-data",
    "build": "next build"
  },
  "dependencies": {
    "@mdx-js/loader": "^3.1.0",
    "@next/mdx": "^16.2.2",
    "next": "^16.2.2",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
